{
    "private": true,
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
       "dev": "cd packages && concurrently \"cd frontend && yarn dev\" \"cd backend && yarn dev\"",
       "sb": "cd services/tera-hub-frontend && yarn sb"
    },
    "dependencies": {
       "concurrently": "^8.2.1"
     }
  }